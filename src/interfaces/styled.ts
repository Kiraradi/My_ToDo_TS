export interface ITheme {
  colors: {
    red: string
    grey: string
    black: string
    white: string
    green: string
  },

  f_size: {
    header: string
    text: string
    b_des: string
    b_mob: string
  }
}